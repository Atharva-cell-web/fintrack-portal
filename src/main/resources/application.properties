# ===================================================================
# FinTrack Application Configuration (Neon Cloud PostgreSQL)
# ===================================================================

# Server Configuration
server.port=8080
spring.application.name=FinTrack

# Database Configuration (Cloud Neon PostgreSQL - Environment Variable Enabled)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-dawn-surf-b3w8z0sf-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_jv8awghiB1bs}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
